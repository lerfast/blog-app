<h1>Posts of <%= @user.name %></h1>

<% if @posts.empty? %>
  <p><%= @user.name %> has no posts.</p>
<% else %>
  <ul>
    <% @posts.each do |post| %>
      <li><%= post.title %></li>
    <% end %>
  </ul>
<% end %>
